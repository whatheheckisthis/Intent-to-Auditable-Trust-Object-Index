{
  "# CONSTRAINT TRADE-OFF": "JSON has no native comment syntax, so required file metadata is encoded as reserved metadata fields.",
  "__file": "orchestration/package.json",
  "__role": "Pinned Node.js dependencies for deterministic orchestration runtime.",
  "__constraints_enforced": [
    "pinned-dependencies",
    "no-dynamic-imports",
    "no-shell-interpolation-scripts"
  ],
  "__upstream": "ROOT",
  "__downstream": "run-execution-orchestration",
  "name": "deterministic-assurance-orchestration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node ./runner.js ./input/runner.payload.json ./runner.schema.json"
  },
  "dependencies": {
    "ajv": "8.17.1"
  },
  "packageManager": "npm@10.9.2"
}
