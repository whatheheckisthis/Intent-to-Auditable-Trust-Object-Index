{
  "# CONSTRAINT TRADE-OFF": "JSON has no native comment syntax, so required file metadata is encoded as reserved metadata fields.",
  "__file": "schemas/webhook-endpoints-secret.schema.json",
  "__role": "Schema for runtime-only webhook endpoint secret manifest.",
  "__constraints_enforced": [
    "secret-manifest-uri-validation",
    "runtime-injection-only",
    "no-additional-properties"
  ],
  "__upstream": "register-webhooks",
  "__downstream": "inject-webhook-endpoints",
  "__ism_controls": ["ISM-0109", "ISM-1554", "ISM-1858"],
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WebhookEndpointsSecret",
  "type": "object",
  "required": [
    "security_operations_url",
    "canary_normaliser_url",
    "sbom_monitor_url"
  ],
  "additionalProperties": false,
  "properties": {
    "security_operations_url": { "type": "string", "format": "uri" },
    "canary_normaliser_url": { "type": "string", "format": "uri" },
    "sbom_monitor_url": { "type": "string", "format": "uri" }
  }
}
